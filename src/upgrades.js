// Config/action migration scripts, run in order when a user upgrades the
// module. Once an entry ships in a published version it must NEVER be
// removed or reordered — Companion persists the executed index per
// connection. v1.2.3 is the first published version, so there is nothing
// to migrate from yet.
module.exports = []
